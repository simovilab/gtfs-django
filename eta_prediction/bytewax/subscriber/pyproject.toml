[project]
name = "databus-mqtt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "paho-mqtt>=2.1.0",
    "redis>=7.0.0",
]

[project.optional-dependencies]
dev = []

[dependency-groups]
dev = [
    "ruff>=0.14.3",
]
